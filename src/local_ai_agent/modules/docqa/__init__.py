"""DocQA — document QA & summarization over local docs.

The first real capability. Loaders turn local files into text inside a `docs_root`
sandbox; later steps chunk, summarize, and answer over that text, exposed as gated tools.
"""
